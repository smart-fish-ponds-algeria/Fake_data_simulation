export enum FastApiRoutes {
  ROOT = "/",
  DISEASE_ENDPOINT = "disease-detection/detect",
  WEIGHT_ENDPOINT = "weight-prediction/predict",
}

export enum ExpressApiRoutes {
  GET_WATER_TANK = "waterTanks",
  Measure = "measurements",
}
